# CNN-Based-Drone-Detection-Models
Drone detection using two deep learning approaches: a custom CNN with multi-scale sliding-window detection and a ResNet18-based fully convolutional grid detector. The project includes custom preprocessing, hyperparameter optimization, Focal Loss, bounding-box regression, transfer learning, and IoU-based evaluation.
